function Birthday(naem) {

let torta = 0.2 * Number(naem);
let napitki = 0.55 * torta;
let animator = Number(naem) / 3
let a = Number(naem) + torta + napitki + animator;

console.log(a)
    
}

Birthday("2250")