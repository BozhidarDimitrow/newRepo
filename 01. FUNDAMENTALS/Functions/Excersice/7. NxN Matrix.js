function solve(num) {
    let num1 = Number(num);

    for (let i = 0; i < num1; i++) {
        let newString = [];
        for (let i = 1; i <= num1; i++) {
        newString.push(num1);        
        }
        console.log(newString.join(" "))
    }



}

solve(2)